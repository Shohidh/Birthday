import { useEffect,useState } from 'react'; import { AnimatePresence,motion } from 'framer-motion'; import { CONFIG } from '../../config/config'; import Envelope from './Envelope';
export default function Letter(){const [open,setOpen]=useState(false),[text,setText]=useState('');useEffect(()=>{if(!open)return;let i=0;setText('');const timer=setInterval(()=>{setText(CONFIG.letter.slice(0,++i));if(i>=CONFIG.letter.length)clearInterval(timer)},18);return()=>clearInterval(timer)},[open]);return <section id="letter" className="px-5 py-24 text-center"><p className="font-bold tracking-[.2em] text-[#819a42]">FOR YOUR HEART</p><h2 className="serif mt-2 text-4xl font-bold">A little letter</h2><div className="mt-8"><AnimatePresence mode="wait">{open?<motion.article key="letter" initial={{opacity:0,rotateX:-30,y:30}} animate={{opacity:1,rotateX:0,y:0}} className="paper mx-auto max-w-xl rounded-sm p-7 text-left leading-8 whitespace-pre-line md:p-10">{text}<span className="animate-pulse">|</span></motion.article>:<motion.div key="envelope" exit={{opacity:0,scale:.8}}><Envelope open={()=>setOpen(true)}/></motion.div>}</AnimatePresence></div></section>}
