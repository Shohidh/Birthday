import { motion } from 'framer-motion'; export default function SunflowerField({onSunflowerClick}){return <div aria-hidden="true" className="pointer-events-auto absolute bottom-0 z-10 flex w-full justify-around overflow-hidden text-5xl md:text-7xl">{Array.from({length:12},(_,i)=><motion.button aria-label="Sunflower" onClick={onSunflowerClick} key={i} className="focus-ring origin-bottom" animate={{rotate:[-3,3,-3]}} transition={{duration:2+i%3,repeat:Infinity,ease:'easeInOut'}} style={{marginBottom:`${(i%3)*-13}px`}}>🌻</motion.button>)}</div>}
