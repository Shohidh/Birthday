import { AnimatePresence,motion } from 'framer-motion'; import { FiChevronLeft,FiChevronRight,FiX } from 'react-icons/fi'; import { CONFIG } from '../../config/config';
export default function PhotoModal({index,setIndex}){const close=()=>setIndex(null);const next=n=>setIndex((index+n+CONFIG.photos.length)%CONFIG.photos.length);return <AnimatePresence>{index!==null&&<motion.div role="dialog" aria-modal="true" aria-label="Photo viewer" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={close} className="fixed inset-0 z-50 grid place-items-center bg-[#1f361a]/85 p-4"><motion.div initial={{scale:.9}} animate={{scale:1}} exit={{scale:.9}} onClick={e=>e.stopPropagation()} className="relative max-w-3xl overflow-hidden rounded-2xl bg-white"><img className="max-h-[75vh] w-full object-contain" src={CONFIG.photos[index].src} alt={CONFIG.photos[index].caption}/><p className="p-4 text-center">{CONFIG.photos[index].caption}</p><button aria-label="Close" onClick={close} className="focus-ring absolute right-3 top-3 rounded-full bg-black/50 p-2 text-white"><FiX/></button><button aria-label="Previous" onClick={()=>next(-1)} className="focus-ring absolute left-3 top-1/2 rounded-full bg-black/50 p-3 text-white"><FiChevronLeft/></button><button aria-label="Next" onClick={()=>next(1)} className="focus-ring absolute right-3 top-1/2 rounded-full bg-black/50 p-3 text-white"><FiChevronRight/></button></motion.div></motion.div>}</AnimatePresence>}
